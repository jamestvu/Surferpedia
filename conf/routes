# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()
GET     /eddie                      controllers.Application.eddie()
GET     /daize                      controllers.Application.daize()
GET     /john                      controllers.Application.john()
GET     /malia                      controllers.Application.malia()
GET     /cjhobgood                      controllers.Application.cjhobgood()
GET     /slater                      controllers.Application.slater()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
